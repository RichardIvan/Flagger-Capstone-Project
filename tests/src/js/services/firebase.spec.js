/* @flow */
'use strict'

import { describe, it, beforeEach } from 'mocha'
import expect from 'expect'

// import firebase from '../../../../src/js/services/firebase'

// describe.only('Firebase Service', () => {
//   it('should firebase is an object', () => {
//     expect(typeof firebase === 'object').toBe(true)
//   })
//   it('should have a signIn method', () => {
//     expect(firebase.signIn).toBeA('function')
//   })
//   it('should return a promise on call', () => {
//     expect(firebase.signIn()).toBeA(Promise)
//   })
// })
